/** 화면(뷰) 식별자. 문자열 리터럴을 흩뿌리지 않기 위해 한곳에 모아 둔다. */
export const VIEWS = {
  LIST: 'list',
  DETAIL: 'detail',
  WRITE: 'write',
  EDIT: 'edit',
}

/** 입력 길이 제한. 폼과 글자수 카운터가 같은 값을 참조한다. */
export const LIMITS = {
  TITLE: 100,
  AUTHOR: 20,
  CONTENT: 2000,
}
